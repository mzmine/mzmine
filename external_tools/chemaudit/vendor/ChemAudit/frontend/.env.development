# ChemAudit Frontend - Development Configuration
# These variables are replaced at BUILD TIME by Vite

# API Base URL for local development
# Backend runs on port 8001, frontend on port 3002
VITE_API_URL=/api/v1
VITE_BACKEND_PORT=8001

# API Documentation URL for development
VITE_API_DOCS_URL=http://localhost:8001/docs

# Allowed hosts for dev server (comma-separated)
# Set this to your server hostname if deploying remotely
VITE_ALLOWED_HOSTS=localhost

# Enable debug logging
VITE_DEBUG=true
