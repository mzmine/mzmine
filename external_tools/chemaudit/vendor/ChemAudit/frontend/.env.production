# ChemAudit Frontend - Production Configuration
# These variables are replaced at BUILD TIME by Vite

# API Base URL for production
# Uses relative URL so frontend and backend can be on same domain
# When deployed behind nginx reverse proxy, /api/v1 routes to backend
VITE_API_URL=/api/v1

# API Documentation URL for production
# Served by backend behind nginx
VITE_API_DOCS_URL=/api/v1/docs

# Allowed hosts for production (comma-separated)
# Not typically needed in production as Vite dev server isn't used
VITE_ALLOWED_HOSTS=localhost

# Disable debug logging in production
VITE_DEBUG=false
